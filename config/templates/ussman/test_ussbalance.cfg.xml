<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE config SYSTEM "file://configuration.dtd">
<!--  "$Id$" -->
<config>
  <section name="test_ussbalance">
    <param name="peerHost" type="string">127.0.0.1</param>
    <param name="peerPort" type="int">9687</param>
  
    <section name="AbonentsDb">
      <section name="1">
        <param name="msAddr" type="string">+79133821781</param>  <!-- mobile sbscriber ISDN address -->
        <param name="inAddr" type="string">+79139860005</param>  <!-- IN (SS provider) ISDN address -->
        <param name="inSSN" type="int">147</param> <!-- IN (SS provider) platform number -->
      </section>
      <section name="2">
        <param name="msAddr" type="string">+79139859489</param>  <!-- mobile sbscriber ISDN address -->
        <param name="inAddr" type="string">+79139860010</param>  <!-- IN (SS provider) ISDN address -->
        <param name="inSSN" type="int">147</param> <!-- IN (SS provider) platform number -->
      </section>
      <section name="3">
        <param name="msAddr" type="string">+79104075344</param>  <!-- mobile sbscriber ISDN address -->
        <param name="inAddr" type="string">+79168960232</param>  <!-- IN (SS provider) ISDN address -->
        <param name="inSSN" type="int">147</param> <!-- IN (SS provider) platform number -->
      </section>
      <section name="4">
        <param name="msAddr" type="string">+79161690056</param>  <!-- mobile sbscriber ISDN address -->
        <param name="inAddr" type="string">+79168960001</param>  <!-- IN (SS provider) ISDN address -->
        <param name="inSSN" type="int">147</param> <!-- IN (SS provider) platform number -->
      </section>

    </section>
  </section>
</config>

