#binary name			source name			libraries
inman/inmanemu	inmanemu -linman -linman-inmanemu -L$(SMSC_BUILDDIR)/bin -lutil -llogger -lutil-leaktracing $(XERCES_LIB) -linman-storage -lcore-network -lsocket -linman-interaction -linman-common -lnsl -linman

