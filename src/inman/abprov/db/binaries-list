# $Id$
# this file contains binaries definitions

# binary name source name libraries         
inman/abprov/db/libinprov_db.so  ProviderDb $(SHARED_OPT) -ldl -ldb -lutil-config -lcore-threads -lpthread $(__LIBRARIES) -lrt
