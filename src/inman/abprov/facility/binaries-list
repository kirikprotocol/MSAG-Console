# $Id$
# this file contains binaries definitions

# binary name source name libraries         
inman/abprov/libinman_iap_fclt.so IAPThrFacility $(SHARED_OPT) -ldl -lutil-config -lcore-threads -lpthread $(__LIBRARIES) -lrt
