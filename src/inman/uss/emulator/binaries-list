#inman/uss/emulator/ussman_emulator ussman_emulator -L$(SMSC_BUILDDIR)/lib $(SOCKETLIB) -linman-uss -linman-interaction -linman-comp-map_uss -linman-codec_inc-map -linman-asn1rt -L$(SS7_STACK_HOME)/lib -linman-inap -linman-inap-map_uss -l$(SS7_LIBRARY_NAME) -linman-inap -linman-comp -linman-common -lutil -lutil-URCdb -lutil-config -lutil-xml -lutil-recoder-recode_dll $(XERCES_LIB) $(__LIBRARIES) -lrt -L$(SMSC_BUILDDIR)/bin -lcore-threads -lthread -llogger -lcore-network $(__LIBRARIES) -lnsl -ldl

inman/uss/emulator/ussman_emulator ussman_emulator -L$(SMSC_BUILDDIR)/lib $(SOCKETLIB) -linman-uss-emulator -linman-uss -linman-interaction -linman-common -lutil -lutil-URCdb -lutil-config -lutil-xml -lutil-xml -lutil-csv -lutil-recoder-recode_dll $(XERCES_LIB) $(__LIBRARIES) -lrt -L$(SMSC_BUILDDIR)/bin -lcore-threads -lthread -llogger -lcore-network $(__LIBRARIES) -lnsl -ldl
