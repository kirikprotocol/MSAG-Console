# $Id$
# this file contains binaries definitions

# binary name                            source name     libraries         

scag/pvss                                mtpers          \
        -lscag-pers-mtpers \
        -lscag-pers-util \
        -lscag-pvss-base \
        -lscag-util-storage \
        -llicense-check \
        -lcore-network $(SOCKETLIB) -lnsl \
        -lcore-threads \
        -lutil-mirrorfile \
        -lutil-config \
        -lutil-xml $(XERCES_LIB) \
        @liblogger.so -L$(SMSC_BUILDDIR)/bin -llogger \
        -lutil \
        $(__LIBRARIES) \
        -lrt -ldl
#scag/client_test/mtpers_client           mtpers_client   @liblogger.so -L$(SMSC_BUILDDIR)/bin -llogger -lscag-pers-mtpers -lscag-pers-util -lutil-mirrorfile -lcore-network -lcore-threads -lrt -lutil -lutil-config -lutil-xml $(XERCES_LIB) $(SOCKETLIB) -lnsl -llogger -llicense-check -lscag-util-storage
