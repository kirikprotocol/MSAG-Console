scag/tests/testsnmp	testsnmp	\
   @liblogger.so -L$(SMSC_BUILDDIR)/bin -llogger \
   $(__LIBRARIES) -lscag-snmp \
   $(if $(filter YES,$(SNMP)),-lnetsnmpmibs -lnetsnmpagent -lnetsnmphelpers -lnetsnmp) \
   -lrt -ldl $(LIBMALLOC)
