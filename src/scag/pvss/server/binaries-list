# $Id$
# this file contains binaries definitions

# binary name                            source name     libraries         

scag/pvss                                pvss          \
        -lscag-pvss-server \
        -lscag-pvss-data \
        -lscag-pvss-base \
	-lscag-pvss-profile \
        -lscag-util-storage \
	-lscag-util-multiplexer \
        -llicense-check \
        -lcore-network $(SOCKETLIB) -lnsl \
        -lcore-threads \
        -lutil-mirrorfile \
        -lutil-config \
        -lutil-xml $(XERCES_LIB) \
        @liblogger.so -L$(SMSC_BUILDDIR)/bin -llogger \
        -lutil \
        $(__LIBRARIES) \
        -lrt -ldl
