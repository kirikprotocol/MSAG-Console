// NOTE: Do NOT edit this file (it is auto-generated).
// Instead, edit message-cpp.tmpl and regenerate.

#ifndef __SCAG_PVSS_PVAP_PC_AUTH_HPP__
#define __SCAG_PVSS_PVAP_PC_AUTH_HPP__

#include "util/int.h"
#include <string>
#include "scag/pvss/api/pvap/Exceptions.h"




#include "scag/pvss/api/packets/AuthRequest.h"





namespace scag2{
namespace pvss{
namespace pvap{

class PVAP;

class PC_AUTH
{
protected:
    static const int protocolVersionTag = 31;
    static const int loginTag = 32;
    static const int passwordTag = 33;
    static const int nameTag = 34;

public:
    PC_AUTH( int seqNum ) :
    owned_(true),
    data_(new AuthRequest(seqNum))
    {
    }

    PC_AUTH( AuthRequest* other ) :
    owned_(false),
    data_(other)
    {
    }

    ~PC_AUTH() {
        if ( data_ && owned_ ) delete data_;
    }

    void clear()
    {
        if ( ! data_ ) return;
        data_->clear();
    }

    AuthRequest* pop() {
        AuthRequest* rv = data_;
        data_ = 0;
        return rv;
    }

    std::string toString() const
    {
        return data_ ? data_->toString() : "";
    }

    template < class DataStream >
        void serialize( const PVAP& proto, DataStream& writer ) const /* throw (PvapException) */ 
    {
        if ( ! data_ ) return;
        checkFields();
        // mandatory fields
        try {
            // printf( "write pos=%d field=%d\n", ds.getPos(), protocolVersionTag );
            writer.writeTag(protocolVersionTag);
            writer.writeByteLV(data_->getProtocolVersion());
        } catch ( exceptions::IOException e ) {
            throw PvapSerializationException( data_->isRequest(),
                                              data_->getSeqNum(),
                                              "writing field protocolVersion in PC_AUTH: %s",
                                              e.what() );
        }
        try {
            // printf( "write pos=%d field=%d\n", ds.getPos(), loginTag );
            writer.writeTag(loginTag);
            writer.writeAsciiLV(data_->getLogin());
        } catch ( exceptions::IOException e ) {
            throw PvapSerializationException( data_->isRequest(),
                                              data_->getSeqNum(),
                                              "writing field login in PC_AUTH: %s",
                                              e.what() );
        }
        try {
            // printf( "write pos=%d field=%d\n", ds.getPos(), passwordTag );
            writer.writeTag(passwordTag);
            writer.writeAsciiLV(data_->getPassword());
        } catch ( exceptions::IOException e ) {
            throw PvapSerializationException( data_->isRequest(),
                                              data_->getSeqNum(),
                                              "writing field password in PC_AUTH: %s",
                                              e.what() );
        }
        try {
            // printf( "write pos=%d field=%d\n", ds.getPos(), nameTag );
            writer.writeTag(nameTag);
            writer.writeAsciiLV(data_->getName());
        } catch ( exceptions::IOException e ) {
            throw PvapSerializationException( data_->isRequest(),
                                              data_->getSeqNum(),
                                              "writing field name in PC_AUTH: %s",
                                              e.what() );
        }
        // optional fields
    }

    template <class DataStream> void deserialize( PVAP& proto, DataStream& reader )
        /* throw (PvapException) */ 
    {
        if ( ! data_ ) return;
        clear();
        int tag = -1;
        try {
            do {
                // int pos = int(reader.getPos());
                tag = reader.readTag();
                // printf( "read pos=%d field=%d\n", pos, tag );
                if ( tag == -1 ) break;
                switch(tag) {
                case protocolVersionTag: {
                    data_->setProtocolVersion(reader.readByteLV());
                    break;
                }
                case loginTag: {
                    data_->setLogin(reader.readAsciiLV());
                    break;
                }
                case passwordTag: {
                    data_->setPassword(reader.readAsciiLV());
                    break;
                }
                case nameTag: {
                    data_->setName(reader.readAsciiLV());
                    break;
                }
                default:
                    throw InvalidFieldTypeException(data_->isRequest(),"PC_AUTH", data_->getSeqNum(),tag);
                }
            } while ( true );
        } catch ( exceptions::IOException e ) {
            throw PvapSerializationException( data_->isRequest(),
                                              data_->getSeqNum(),
                                              "reading field tag=%d of PC_AUTH: %s",
                                              tag, e.what() );
        }
        checkFields();
    }

    uint32_t getSeqNum() const {
        return data_ ? data_->getSeqNum() : uint32_t(-1);
    }

protected:
    void checkFields() const /* throw (PvapException) */ 
    {
        // using parent check
        if ( !data_->isValid() ) {
            throw MessageIsBrokenException(data_->isRequest(), data_->getSeqNum(), "message PC_AUTH is broken: %s",data_->toString().c_str());
        }
    }

private:
    PC_AUTH( const PC_AUTH& );
    PC_AUTH& operator = ( const PC_AUTH& );

protected:
    //static const uint8_t versionMajor=2;
    //static const uint8_t versionMinor=0;

    bool                owned_;
    AuthRequest* data_;

};

} // namespace scag2
} // namespace pvss
} // namespace pvap
#endif
