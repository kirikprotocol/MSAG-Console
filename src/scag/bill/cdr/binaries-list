# $Id$
# this file contains binaries definitions

# binary name        source name	libraries         
scag/bill/libcdr.so   CDRBillingMachine $(SHARED_OPT)  -lscag-bill-cdr -lscag-re-actions -lutil -lcore-threads -lrt  