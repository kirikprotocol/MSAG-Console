# $Id$
# this file contains binaries definitions

# binary name source name libraries         

#old (EINss7166)
mcisme/MCISme MCISme \
    -ldl -ladmin-util -ladmin-protocol \
    -ladmin-service -ladmin-util -ladmin-protocol -ladmin-daemon -ladmin \
    -lsystem-common -lutil-config-route $(SOCKETLIB) -lcore-network -lmcisme \
    -lmcisme-misscall -lmcisme-advert -lmcisme -lscag-util-singleton \
    -lscag-exc -lscag-util-encodings -lscag-util-singleton -lscag-exc \
    -lutil-regexp -L$(SS7_STACK_HOME)/lib -l$(SS7_LIBRARY_NAME) \
    -lmcisme-misscall -lmcisme-mcaia -ldb -lutil-templates -lutil-config \
    -lutil-xml -lutil -lutil-recoder-recode_dll -lcore-threads -lcore-buffers \
    -lthread $(XERCES_LIB) -lsms $(__LIBRARIES) -lrt -L$(SMSC_BUILDDIR)/bin \
    -llogger -lnsl -lsystem-common -lutil-config-route
#mcisme/DatFileConvert DatFileConvert \
    -ldl -ladmin-util -ladmin-protocol \
    -ladmin-service -ladmin-util -ladmin-protocol -ladmin-daemon -ladmin \
    -lsystem-common -lutil-config-route $(SOCKETLIB) -lcore-network -lmcisme \
    -lmcisme-misscall -lmcisme-mcaia -lutil-regexp -L$(SS7_STACK_HOME)/lib \
    -l$(SS7_LIBRARY_NAME) -lmcisme-misscall -ldb -lutil-templates \
    -lutil-config -lutil-xml -lutil -lutil-recoder-recode_dll -lcore-threads \
    -lcore-buffers -lthread $(XERCES_LIB) -lsms $(__LIBRARIES) \
    -lrt -L$(SMSC_BUILDDIR)/bin -llogger -lnsl
#mcisme/dbStat2Files dbStat2Files \
    -ldl -lmcisme -lmcisme-misscall \
    -lutil-regexp -L$(SS7_STACK_HOME)/lib -l$(SS7_LIBRARY_NAME) \
    -lmcisme-misscall -ldb -lutil-templates -lutil-config -lutil-xml \
    -lutil -lutil-recoder-recode_dll -lcore-threads -lcore-buffers \
    -lthread $(XERCES_LIB) -lsms $(__LIBRARIES) -lrt \
    -L$(SMSC_BUILDDIR)/bin -llogger -lnsl
#mcisme/MCIProfdb2file MCIProfdb2file \
    -ldl -lmcisme -lmcisme-misscall \
    -lutil-regexp -L$(SS7_STACK_HOME)/lib -l$(SS7_LIBRARY_NAME) \
    -lmcisme-misscall -ldb -lutil-templates -lutil-config -lutil-xml \
    -lutil -lutil-recoder-recode_dll -lcore-threads -lcore-buffers \
    -lthread $(XERCES_LIB) -lsms $(__LIBRARIES) -lrt \
    -L$(SMSC_BUILDDIR)/bin -llogger -lnsl
mcisme/DatFileConvert datfileconvert

#test (no stack)
#mcisme/MCISme	MCISme \
    -ldl -ladmin-util -ladmin-protocol -ladmin-service \
    -ladmin-util -ladmin-protocol -ladmin-daemon -ladmin \
    $(SOCKETLIB) -lcore-network -lmcisme -ldb -lutil-templates \
    -lutil-config -lutil-xml -lutil -lutil-recoder-recode_dll -lcore-threads \
    -lcore-buffers -lthread $(XERCES_LIB) -lsms $(__LIBRARIES) \
    -lrt -L$(SMSC_BUILDDIR)/bin -llogger -lnsl
