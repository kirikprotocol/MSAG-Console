# $Id$
# this file contains binaries definitions

# binary name        source name  libraries
-acls/acls acls -lcore-network -lutil -lsms -lutil -lsocket -lcore-threads -lthread -lrt -lutil-recoder-recode_dll -lutil-config -L$(ORACLE_LIB) -lutil-xml $(__LIBRARIES) $(XERCES_LIB) -ldb -lemailsme-util -lutil-templates -lnsl -ldl
