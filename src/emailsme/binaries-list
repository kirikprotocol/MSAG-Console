# $Id$
# this file contains binaries definitions

# binary name        source name	libraries
emailsme/mdAgent mdAgent -lcore-buffers -lcore-network -lsocket
emailsme/emailsme emailsme -lcore-network -lutil -lsms -lutil -lsocket -lcore-threads -lthread -lrt -lutil-recoder-recode_dll -lutil-config -L$(ORACLE_LIB) -lutil-xml -llog4cpp -lxerces-c -lutil -lutil-regexp
emailsme/retest retest -lutil-regexp
