# $Id$
# this file contains binaries definitions

# binary name source name libraries         
db/oci/libdb_oci.so  OCIDataSource $(SHARED_OPT) -ldl -ldb -lutil-config -lutil-xml -lutil -lcore-threads -lpthread $(__LIBRARIES) -lrt
