# $Id$
# this file contains binaries definitions

# binary name source name libraries         
db/oci/libdb_oci.so  OCIDataSource $(SHARED_OPT) -ldl -ldb \
 -lutil-config -lutil-xml -lutil -lcore-threads -lpthread \
 -L$(ORACLE_LIB) -lclntsh \
 @db/exceptions/libdb_exc.so \
 -L$(SMSC_BUILDDIR)/bin/db/exceptions -ldb_exc -L$(SMSC_BUILDDIR)/bin -llogger -lrt
