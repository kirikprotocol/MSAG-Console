# $Id$
# this file contains binaries definitions

# binary name        source name	libraries
-util/xml/test	test -lutil-xml $(XERCES_LIB) -L$(SMSC_BUILDDIR)/bin -llogger $(__LIBRARIES)
-util/xml/test2	test2 -lrt -L$(SMSC_BUILDDIR)/bin -llogger
