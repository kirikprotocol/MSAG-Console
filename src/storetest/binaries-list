# $Id$
# this file contains binaries definitions

# binary name        source name	libraries         
storetest/StoreTest  StoreTest          -L$(ORACLE_HOME)/lib -lstore -lstore-oci $(__OBJECTS) $(__LIBRARIES)
