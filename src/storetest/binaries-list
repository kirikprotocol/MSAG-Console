# $Id$
# this file contains binaries definitions

# binary name        source name	libraries         
storetest/StoreTest  StoreTest          -L$(ORACLE_LIB) -lstore -lsms -lutil-config -lutil-xml -lutil -llogger -lcore-threads -lthread $(XERCES_LIB) $(__LIBRARIES) -lrt
