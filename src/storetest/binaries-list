# $Id$
# this file contains binaries definitions

# binary name        source name	libraries         
storetest/StoreTest  StoreTest          -L$(ORACLE_LIB) -lstore -lsms -lutil-config -lutil-xml -lutil -llog4cpp -lcore-threads -lthread -lxerces-c $(__LIBRARIES) -lrt
