#
# "@(#)$Id$"
#
# PROD_PREFIX = product name prefix, is added to name of build NUM and DATE defines
# PROD_VER_FILE = relative to $(SMSC_SRCDIR)/$(PACKAGE) name of file from CVS
#                 repository to extract BuildId tag
# BUILDID_HEADER = relative to $(SMSC_SRCDIR)/$(PACKAGE) name of .hpp file to write build NUM and DATE defines

PROD_PREFIX = SCALABLE_SMSC
PROD_VER_FILE = version.cpp
BUILDID_HEADER = scalableSmscBuildId.hpp
SMSC_BUILD_TAG_FORMAT=S[0-9]+
GENERATE_VERSIONS = true
