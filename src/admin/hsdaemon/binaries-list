# $Id$
# this file contains binaries definitions

# binary name			source name			libraries																		description
admin/daemon/hs_smsc_ssdaemon	main \
  -ladmin-hsdaemon -ladmin-util -ladmin \
  -ladmin-protocol -ladmin-service \
  -lutil-config -lutil-xml -lutil \
  -lcore-threads -lcore-buffers -lcore-network \
  $(XERCES_LIB) $(SOCKETLIB) $(LIBICONV) -lpthread \
  @liblogger$(SHLIBEXT) -L$(SMSC_BUILDDIR)/bin -llogger \
  $(LIBNSL) $(LIBRT) -lutil-mirrorfile
admin/daemon/hsadmin hsadmin \
  -lcore-network $(SOCKETLIB) $(LIBNSL) $(LIBRT) -lutil -lutil-xml $(XERCES_LIB)
