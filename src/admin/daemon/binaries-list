# $Id$
# this file contains binaries definitions

# binary name			source name			libraries																		description
admin/daemon/smsc_ssdaemon	main				-ladmin-daemon -ladmin-util -ladmin -ladmin-protocol -ladmin-service	-lutil-config -lutil-xml -lutil 	-lcore-threads -lcore-buffers -lcore-network 	-llog4cpp -lxerces-c -lsocket -lthread
