# $Id$
# this file contains binaries definitions

# binary name source name libraries         

#test ()
cluster/CommandsTest   	  CommandsTest	 -ldl $(SOCKETLIB) -lcore-network -lcluster-listeners -lmscman -lcluster -lutil -lcore-threads -lcore-buffers -lthread $(XERCES_LIB) -lsms $(__LIBRARIES) -lrt -L$(SMSC_BUILDDIR)/bin -llogger -lnsl