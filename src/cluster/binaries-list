# $Id$
# this file contains binaries definitions

# binary name source name libraries         

#test ()
cluster/agent  agent	 -ldl $(SOCKETLIB) -lcore-network -lcluster-listeners -lmscman -lcluster -lsystem -lutil -lutil-config -lutil-xml -lcore-threads -lcore-buffers -lthread $(XERCES_LIB) -lsms $(__LIBRARIES) -lrt -L$(SMSC_BUILDDIR)/bin -llogger -lnsl